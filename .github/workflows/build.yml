name: Build

on:
  push:
    branches: [main]
    # Both shapes, deliberately. The convention is "v1.2.3", but the 1.0.2
    # release was tagged "1.0.2" and a tag that matches no filter starts no run
    # at all: no job, no red cross, no notification, just a release with nothing
    # attached to it. A missing "v" must build, not vanish. The update check
    # trims a leading "v" already (internal/update.Compare), so either name
    # works everywhere else.
    # 'v*' is left exactly as it was rather than tidied into one cleverer
    # pattern: it is the one filter proven to work in production (v1.0.0 and
    # v1.0.1 both built and released from it). The second entry is added
    # alongside it, so the worst case if a character class behaves differently
    # from what the docs say is today's behaviour, never less.
    tags: ['v*', '[0-9]*']
  pull_request:
    branches: [main]

permissions:
  contents: read

env:
  WAILS_VERSION: v2.13.0

jobs:
  build:
    name: ${{ matrix.name }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            name: Windows
            artifact: chit-windows-amd64
          - os: macos-latest
            name: macOS
            artifact: chit-macos-universal
          - os: ubuntu-latest
            name: Linux
            artifact: chit-linux-amd64

    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: true

      # 24, not 22: the frontend tests are TypeScript on Node's built-in test
      # runner, which needs type stripping to be on by default.
      - uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      # Arch and Ubuntu 24.04 ship webkit2gtk-4.1 only, which is why every
      # Linux build of this app carries the webkit2_41 tag.
      - name: Install Linux build dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential pkg-config libgtk-3-dev libwebkit2gtk-4.1-dev

      - name: Install Wails
        run: go install github.com/wailsapp/wails/v2/cmd/wails@${{ env.WAILS_VERSION }}

      # Runs before the Go steps: main.go embeds frontend/dist, so it has to
      # exist before anything compiles.
      - name: Build frontend
        working-directory: frontend
        run: |
          npm ci
          npm run build

      - name: Frontend tests
        working-directory: frontend
        run: npm test

      - name: go vet
        run: go vet ./...

      # -short skips the tests that need a real network; a CI runner has no LAN
      # to sweep and usually no unprivileged ICMP.
      - name: go test
        run: go test -short ./...

      - name: Build app (Windows)
        if: runner.os == 'Windows'
        run: wails build -clean -s -platform windows/amd64

      - name: Build app (macOS)
        if: runner.os == 'macOS'
        run: wails build -clean -s -platform darwin/universal

      - name: Build app (Linux)
        if: runner.os == 'Linux'
        run: wails build -clean -s -tags webkit2_41 -platform linux/amd64

      - name: Package (Windows)
        if: runner.os == 'Windows'
        run: Copy-Item build/bin/chit.exe ${{ matrix.artifact }}.exe

      - name: Package (macOS)
        if: runner.os == 'macOS'
        run: ditto -c -k --keepParent build/bin/chit.app ${{ matrix.artifact }}.zip

      - name: Package (Linux)
        if: runner.os == 'Linux'
        run: tar -czf ${{ matrix.artifact }}.tar.gz -C build/bin chit

      - uses: actions/upload-artifact@v7
        with:
          name: ${{ matrix.artifact }}
          path: |
            ${{ matrix.artifact }}.exe
            ${{ matrix.artifact }}.zip
            ${{ matrix.artifact }}.tar.gz
          if-no-files-found: error

  release:
    name: Release
    needs: build
    # Any tag, because the trigger filter above already decides which tags start
    # a run. Keying this on "refs/tags/v" was the second half of the 1.0.2
    # fault: even had the run started, the release job would have skipped.
    if: startsWith(github.ref, 'refs/tags/')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v8
        with:
          path: dist
          merge-multiple: true

      - uses: softprops/action-gh-release@v3
        with:
          files: dist/*
          draft: true
          generate_release_notes: true
